<div class="gallery">
    <h1>GALERÍA DE FOTOS</h1>
    {{#if boolean}}
        <div class="gallery-box">
          {{#each photos as |photo|}}
            <img src="{{this.image}}">
          {{/each}}  
        </div>
    {{else}}
        <p>No hay fotos cargadas.</p>
    {{/if}}
</div>